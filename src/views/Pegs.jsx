import { VStack } from "@chakra-ui/layout";
import React from "react";
import PegsList from "../components/PegsList";

const Pegs = () => {
  return (
    <VStack>
      <PegsList />
    </VStack>
  );
};

export default Pegs;
